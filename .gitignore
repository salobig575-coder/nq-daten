# Die Rohdaten werden bei jedem Lauf neu geholt und muessen nicht in der
# Git-Historie liegen. Wuerden sie committet, waechst das Repo um mehrere
# Gigabyte pro Monat. Committet werden nur die kleinen JSON-Ergebnisse.
data/*.csv
__pycache__/
*.pyc
